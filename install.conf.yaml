- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.dotfiles: ''
    ~/.ssh/config: ssh
    ~/.vimrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
