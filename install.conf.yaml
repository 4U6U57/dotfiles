- defaults:
    link:
      create: true
      relink: true

- clean: 
  - '~'
  - '.ssh'

- link:
    ~/.bash_profile:
    ~/.dotfiles: ''
    ~/.ssh/config: ssh
    ~/.vimrc:

- shell:
  - [git pull origin master, Updating repo]
  - [git submodule update --init --recursive, Installing submodules]
