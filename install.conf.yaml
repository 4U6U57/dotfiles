# Config file for dotbot
# August Valera (@4U6U57)
# Run ./install to run dotbot

- defaults:
    link:
      create: true
      relink: true

- clean: 
  - '~'
  - '~/.ssh'
  - '~/bin'

- link:
    ~/bin/crontab-edit: crontab-edit.sh
    ~/bin/duck: duck.sh
    ~/.bash_aliases:
    ~/.bash_profile:
    ~/.bash_prompt:
    ~/.bashrc:
    ~/.crontab:
    ~/.dotfiles: ''
    ~/.editorconfig:
    ~/.gitconfig:
    ~/.ssh/config: ssh
    ~/.vimrc:

- shell:
  - [git pull origin master, Updating repo]
  - [git submodule update --init --recursive, Installing submodules]
  - [crontab ~/.crontab, Configuring crontab]
